//! Preview→Stable config-directory migration is Warp-only. PrompTTY has a
//! single public identity, so this is a no-op kept for the existing call site.

pub(crate) fn migrate_preview_config_dir_if_needed() {}
